name: Deploy to VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to VPS via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SSH_HOST }}
          username: root
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          script: |
            # تحديد مسار المشروع (المسار الذي وجدناه سابقاً)
            cd /root/4you-store
            
            # سحب الكود الجديد
            git pull origin main
            
            # تثبيت المكتبات والبناء
            pnpm install
            pnpm run build
            
            # إعادة تشغيل الموقع عبر pm2
            # إذا لم يكن موجوداً، سيقوم بتشغيله لأول مرة
            pm2 restart 4you-store || pm2 start dist/index.js --name 4you-store
