{
  "version": 2,
  "buildCommand": "pnpm install && vite build && esbuild server/vercel.ts --platform=node --packages=external --bundle --format=esm --outfile=api/server.js --alias:@shared=./shared",
  "outputDirectory": "dist/public",
  "installCommand": "pnpm install",
  "framework": null,
  "functions": {
    "api/server.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/server.js"
    },
    {
      "source": "/((?!api).*)",
      "destination": "/index.html"
    }
  ]
}
